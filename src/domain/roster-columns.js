const memberColumn = {
  name: 'name',
  required: true,
  label: 'Miembro',
  align: 'left',
  field: 'name',
  sortable: true
}

const unitNameColumn = {
  name: 'unitName',
  required: true,
  label: 'Unidad',
  align: 'left',
  field: 'unitName',
  sortable: true
}

const starsColumn = {
  name: 'stars',
  required: true,
  label: 'Estrellas',
  align: 'left',
  field: 'stars',
  sortable: true
}

const levelColumn = {
  name: 'level',
  required: true,
  label: 'Nivel',
  align: 'left',
  field: 'level',
  sortable: true
}

const gearColumn = {
  name: 'gear',
  required: true,
  label: 'Equipo',
  align: 'left',
  field: 'gear',
  sortable: true
}

const powerColumn = {
  name: 'power',
  required: true,
  label: 'Poder',
  align: 'left',
  field: 'power',
  sortable: true
}

const zetasColumn = {
  name: 'zetas',
  required: true,
  label: 'Zetas',
  align: 'left',
  field: 'zetas',
  sortable: true
}

const guildNameColumn = {
  name: 'guild',
  required: true,
  label: 'Gremio',
  align: 'left',
  field: 'guild',
  sortable: true
}

export default [
  memberColumn,
  unitNameColumn,
  starsColumn,
  levelColumn,
  gearColumn,
  powerColumn,
  zetasColumn,
  guildNameColumn
]
