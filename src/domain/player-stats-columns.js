const memberColumn = {
  name: 'name',
  required: true,
  label: 'Miembro',
  align: 'left',
  field: 'name',
  sortable: true
}

const guildNameColumn = {
  name: 'guild',
  required: true,
  label: 'Gremio',
  align: 'left',
  field: 'guild',
  sortable: true
}

const healthBColumn = {
  name: 'healthB',
  required: true,
  label: 'Salud II (16)',
  align: 'center',
  field: 'healthB',
  sortable: true
}

const healthCColumn = {
  name: 'healthC',
  required: true,
  label: 'Salud III (8)',
  align: 'center',
  field: 'healthC',
  sortable: true
}

const healthDColumn = {
  name: 'healthD',
  required: true,
  label: 'Salud IV (4)',
  align: 'center',
  field: 'healthD',
  sortable: true
}

const healthEColumn = {
  name: 'healthE',
  required: true,
  label: 'Salud V (1)',
  align: 'center',
  field: 'healthE',
  sortable: true
}

const speedAColumn = {
  name: 'speedA',
  required: true,
  label: 'Velocidad I (32)',
  align: 'center',
  field: 'speedA',
  sortable: true
}

const speedBColumn = {
  name: 'speedB',
  required: true,
  label: 'Velocidad II (16)',
  align: 'center',
  field: 'speedB',
  sortable: true
}

const speedCColumn = {
  name: 'speedC',
  required: true,
  label: 'Velocidad III (8)',
  align: 'center',
  field: 'speedC',
  sortable: true
}

const speedDColumn = {
  name: 'speedD',
  required: true,
  label: 'Velocidad IV (4)',
  align: 'center',
  field: 'speedD',
  sortable: true
}

const speedEColumn = {
  name: 'speedE',
  required: true,
  label: 'Velocidad V (1)',
  align: 'center',
  field: 'speedE',
  sortable: true
}

const protectionBColumn = {
  name: 'protectionB',
  required: true,
  label: 'Protección II (16)',
  align: 'center',
  field: 'protectionB',
  sortable: true
}

const protectionCColumn = {
  name: 'protectionC',
  required: true,
  label: 'Protección III (8)',
  align: 'center',
  field: 'protectionC',
  sortable: true
}

const protectionDColumn = {
  name: 'protectionD',
  required: true,
  label: 'Protección IV (4)',
  align: 'center',
  field: 'protectionD',
  sortable: true
}

const protectionEColumn = {
  name: 'protectionE',
  required: true,
  label: 'Protección V (1)',
  align: 'center',
  field: 'protectionE',
  sortable: true
}

const damageBColumn = {
  name: 'damageB',
  required: true,
  label: 'Daño II (16)',
  align: 'center',
  field: 'damageB',
  sortable: true
}

const damageCColumn = {
  name: 'damageC',
  required: true,
  label: 'Daño III (8)',
  align: 'center',
  field: 'damageC',
  sortable: true
}

const damageDColumn = {
  name: 'damageD',
  required: true,
  label: 'Daño IV (4)',
  align: 'center',
  field: 'damageD',
  sortable: true
}

const damageEColumn = {
  name: 'damageE',
  required: true,
  label: 'Daño V (1)',
  align: 'center',
  field: 'damageE',
  sortable: true
}

const scoreColumn = {
  name: 'score',
  required: true,
  label: 'Puntuación Final',
  align: 'left',
  field: 'score',
  sortable: true
}

export default [
  memberColumn,
  guildNameColumn,

  healthBColumn,
  healthCColumn,
  healthDColumn,
  healthEColumn,

  speedAColumn,
  speedBColumn,
  speedCColumn,
  speedDColumn,
  speedEColumn,

  protectionBColumn,
  protectionCColumn,
  protectionDColumn,
  protectionEColumn,

  damageBColumn,
  damageCColumn,
  damageDColumn,
  damageEColumn,

  scoreColumn
]
